namespace Sample
{
    internal class Program
    {
        static void Main(string[] args)
        {
            /*
            int[] intArray = { 3, 2, 1, 4, };
            foreach (int number in intArray)
            {
                intArray[3] = 1;
            }
            Console.WriteLine($"{intArray[0]}, {intArray[1]}, {intArray[2]}, {intArray[3]}");
        
            Console.ReadLine();
            
            /*
       int[] intArray = new int[] { 2, 9, 4, 3, 5, 1, 7 };
       int temp = 0;
       for (int i = 0; i <= intArray.Length; i++)
       {
           for (int j = i+1; j < intArray.Length - 1; j++)
           {
               if (intArray[i] > intArray[j])
               {
                   temp = intArray[i];
                   intArray[i] = intArray[j];
                   intArray[j] = temp;
               }
           }
       }
       Console.WriteLine("Array sorted in assencing order");
       foreach (var item in intArray)
       {
           Console.WriteLine(item);
       }
       */
        }

    }
}